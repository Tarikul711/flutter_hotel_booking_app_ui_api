import 'package:flutter/foundation.dart';

class Destination {
  final String title, image, slug;

  Destination({
    @required this.title,
    @required this.slug,
    @required this.image,
  });
}
